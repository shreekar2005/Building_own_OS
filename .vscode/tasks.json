{
    "version": "2.0.0",
    "tasks": [
        {
            // For Debug button in VSCode

            "label": "Clear, Build and Run QEMU",
            "type": "shell",
            "command": "cd OSOS && make clear && make dbg_start_qemu",
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}